# 互動回歸檢查

本文件用於每次修改網站後的固定確認。先執行自動檢查，再用瀏覽器完成互動檢查；不需要登入或保留學生使用紀錄。

## 自動冒煙測試

在專案根目錄執行：

```powershell
powershell -ExecutionPolicy Bypass -File tests/check-course.ps1
```

它會確認四個單元都有共用導覽、學習目標掛載點、課後回顧備援、互動回饋，以及三項重點互動所需的程式碼。這是結構與關鍵功能的快速警報，不取代實際操作。

## 每次修改後的人工檢查

| 範圍 | 操作 | 預期結果 |
| --- | --- | --- |
| 全站 | 開啟首頁與單元一至四；點擊「課程地圖」和各單元按鈕 | 導覽可到正確頁面；目前單元有主色調標示。 |
| 全站 | 捲到學習目標、課後回顧與同伴討論 | 每個單元都有三項目標、三題檢核與一題討論。 |
| 單元一 | 開始 BINARY BONANZA，使用滑鼠/觸控拖拉三顆相連寶石；也用 Tab、Enter 或空白鍵選取 | 計時、插槽、相鄰提醒與驗證回饋都可運作。 |
| 單元二 | 完成一次聽音解碼與一次發報驗證；故意少打一個空白 | 成功會顯示完成訊息並停止計時；錯誤會給出對應提示。 |
| 單元三 | 切換聲音格式比較並播放；調整取樣或格式控制 | 內容、狀態文字和按鈕狀態會同步變更；無法播放時畫面仍可閱讀。 |
| 單元四 | 使用自由畫布，再以色票和鍵盤在 8×8 格上色；比較結果後返回修改 | 色票、格子、清除與比較視窗都可操作；Enter/空白鍵可上色。 |
| 課後區塊 | 每題選答一次，答完三題；選擇討論立場 | 分數、徽章與討論提示會更新，且螢幕閱讀器可取得回饋。 |
| 行動裝置 | 用窄螢幕或手機重做單元一、二、四的關鍵操作 | 導覽可水平滑動，按鈕不被截斷，觸控操作可用。 |

## 集中設定的維護原則

- 修改單元導覽、學習目標、課後題目或討論題時，優先編輯 `course-data.js`。
- `course-core.js` 負責把設定套用到各單元；`course-components.css` 負責共用外觀。
- 每個 HTML 頁仍保有靜態內容與基本互動備援。若共用腳本無法載入，學生仍能閱讀內容、完成基本檢核與討論。
